【执行文件夹 = D:\系统默认\桌面\畅密\tuku】
（存链项目全部代码在此目录：index.js、wrangler.jsonc、schema.sql、migration-v2.sql、migration-v3.sql、scripts/verify.mjs、docs/*；仓库 jxgcr/tuku。只审这个目录，不要动/不要参照 D:\系统默认\桌面\安全账户、D:\系统默认\桌面\安全池子、以及 畅密 下的其它子项目——无关的独立仓库。）

# 角色
你是独立验收员。此前一轮第三方审查报出若干问题，随后开发者声称已整改。你的任务是**独立核验每个问题是否真正修复**，不采信开发者的任何结论与代码注释，一切以代码实际行为和运行时实测为准。

# 项目事实（背景）
- 多租户文件/图片托管 SaaS（Cloudflare Worker + D1 + R2 + CF Images + Durable Object）。
- 前端是 worker 内反引号模板字符串（PAGE_HTML 客户端、ADMIN_HTML 运营台 /scfw），下发时求值；审前端 JS 以“求值后实际下发”的 `<script>` 为准。
- 客户端 link.aistela.com，worker tuku.5209696.xyz，运营台 /scfw。
- 部分实测需要凭证：登录后流程需一张畅密测试卡；运营台需 ADMIN_KEY。**若未获得，请在对应条目标注“因缺凭证未实测”，不要臆断。**
- 注意：`#4` 的对账 Cron 依赖 D1 表 `pending_deletes`；请先确认该表是否已在库中（`migration-v3.sql` / `schema.sql` 定义），未建表时相关代码应为“安全空转”。

# 需要逐条核验的问题（判定：已修复 / 部分修复 / 未修复 / 因缺凭证未实测；每条给文件:行号或实测证据）
1. **分片上传配额可绕过**：过去 mpu 完成时用客户端申报的 size 记账，可申报极小值绕配额、真实占满 R2。核验：complete 是否以服务端/存储真实大小为准记账并复核配额、超限是否回滚。黑盒（需卡）：mpu/create 申报 size=1、实传约 1MB、complete 仍报 size=1，检查该文件入库 bytes 是否为真实大小而非 1。
2. **普通上传配额并发竞态（TOCTOU）**：先查用量再插入、非原子，并发可超卖。核验：插入后是否复核总量并在超限时回滚。
3. **schema.sql 与运行时脱节**：过去缺 byte_limit / kind / r2_key / mime，且只按注释建表会导致开通 INSERT 失败。核验：按当前 schema.sql 从零建库能否让开通与上传跑通（可用本地 `--local` D1 验证）。
4. **删除/上传失败留下孤儿存储对象**：过去存储删除失败仍删 DB 行、上传写库失败不回滚存储。核验：删除失败是否登记待清理、上传写库失败是否回滚已存对象、是否有定时任务补删（scheduled + cron 配置 + pending_deletes 表）。
5. **登录账号枚举**：过去未知卡与密码错返回不同状态码/文案，可区分卡是否已开通。黑盒：用随机卡号 + 任意密码打 /api/login，再对比“已存在卡 + 错密码”，两者状态码与文案是否已一致。
6. **弱口令 + 仅 IP 暴破锁**：核验密码强度下限是否已提高、是否只在“开通设密码”时校验（不影响老用户登录）、是否新增了按卡号维度的失败锁。
7. **CSP 放开内联脚本（unsafe-inline）**：黑盒：`curl -I https://tuku.5209696.xyz/` 看 `content-security-policy` 的 `script-src` 是否已去掉 `unsafe-inline`（改用 nonce 或外链），并确认页面脚本仍能正常执行（登录后前端功能可用、无 CSP 报错）。同时核验 `esc()` 是否转义引号。
8. **上游调用无超时**：核验对畅密验卡、CF Images 的 fetch 是否加了超时与失败降级（不会挂起到 Worker 上限）。
9. **文件上传被当可执行内容渲染（存储型 XSS）**：黑盒（需卡）：上传一个 `text/html` 文件，取其直链，检查响应是否 `Content-Disposition: attachment` + `nosniff`（不内联执行）；再上传 `video/mp4` 确认安全类型仍可内联预览。
10. **文件直链可枚举（IDOR）**：黑盒：确认文件直链形如 `/f/<id>~<令牌>`；请求裸 `/f/<id>` 与错误令牌是否都拿不到文件（非 200），正确带令牌是否 200。
11. **部署闸/回滚文档**：核验 `scripts/verify.mjs` 是否新增了“求值前端模板 → node --check 下发脚本”的检查（能否拦住模板转义导致的前端 JS 语法错）；是否有运维恢复文档（D1 time-travel / wrangler rollback / 备份）。

# 方法与证据要求
- 每条给判定 + 文件:行号或实测输出；区分“已确认”与“疑似/未实测”。
- 不采信注释与开发者结论，以代码实际行为、运行时实测为准。
- 白盒：审 index.js 等；前端以求值后 `<script>` 为准（可参考 scripts/verify.mjs 的求值方式）。
- 黑盒：可对 tuku.5209696.xyz / link.aistela.com / /scfw 实测；**不要对生产做破坏性写入**（如需破坏性验证请说明并改在测试号/`--local` 上做）。

# 输出格式
1. 汇总表：编号 | 判定(已修复/部分/未修复/未实测) | 关键证据(文件:行 或 实测结果)。
2. 仍未修复或修复不彻底的条目：详述问题与复现。
3. 本轮整改中你发现的**新引入问题/回归**（如有）。
4. 因缺凭证/环境未能验证的条目清单，说明还需要什么。

请开始，只讲能被证据支撑的判断。
